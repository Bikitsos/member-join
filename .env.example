# Mailgun Email Configuration
# Copy this file to .env.mailgun and update with your actual values

# Your Mailgun domain (e.g., mg.yourdomain.com or your-sandbox-domain.mailgun.org)
MAILGUN_DOMAIN=your-domain.mailgun.org

# Your Mailgun API key (starts with "key-")
MAILGUN_API_KEY=key-your-api-key-here

# The email address to send from (must be authorized in your Mailgun domain)
FROM_EMAIL=noreply@your-domain.com

# Application configuration
DATABASE_PATH=members.db
HOST=localhost
SHOW_BROWSER=true

# Example usage:
# 1. Copy this file: cp .env.example .env.mailgun
# 2. Edit .env.mailgun with your actual Mailgun credentials
# 3. Run with environment: set -a; source .env.mailgun; set +a; uv run main.py